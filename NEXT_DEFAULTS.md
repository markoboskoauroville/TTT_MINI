# NEXT_DEFAULTS — moved

The reasoning, the rejected alternatives and every bug are now in
[`DEVELOPMENT.md`](DEVELOPMENT.md), split away from [`HANDOFF.md`](HANDOFF.md) so that the handoff is
the finished state and nothing else.

Nothing was lost. This file's whole contents are Part Three of `DEVELOPMENT.md`, unchanged and in the
same order, with the bugs re-grouped **by shape** above it.

**This stub is deliberate.** `MANTRA_MANIFEST`, this repository's own older documents and several
past handoffs tell a session to read `NEXT_DEFAULTS.md` by name. A session that follows that
instruction and finds nothing would rebuild decisions that have already been paid for twice.

**Do not delete it.** It costs five lines and it is load-bearing for exactly as long as anything
still points here, which is longer than anybody will remember.
