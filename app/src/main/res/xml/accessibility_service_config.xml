<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuration for DictateAccessibilityService (issue #88, floating dictation button).

  canRetrieveWindowContent is required to read the focused field (to detect that it is editable and
  to insert the transcribed text at the cursor). The event types are limited to focus/selection/window
  changes so the service only wakes up when the input focus might have moved. typeWindowContentChanged
  is deliberately NOT subscribed: it fires on every character typed, and each one made the service
  re-fetch the focused node's full AccessibilityNodeInfo (text + spans) — a per-keystroke IPC flood that
  caused visible typing jank. Focus never moves on a content change, so nothing is lost by ignoring it.
  notificationTimeout stays at zero: the service itself coalesces the only high-frequency event
  (typeViewTextSelectionChanged), while focus and window changes must reach the bubble immediately.
  canPerformGestures is what lets the pointer click. dispatchGesture sends a real touch into whichever
  app is in front, so that app reacts exactly as it would to a finger: menus open, dialogs appear,
  lists scroll. That liveness is the whole reason the feature is built around a pointer rather than
  around watching the user's own finger, which Android does not permit while the app underneath is
  still responding.

  canRequestFilterKeyEvents was added at build 124 and REMOVED at build 131, and must not come back
  without a way to test it first.

  Declaring it made the whole service unusable on a Nothing Phone 2a running Android 16: the
  accessibility toggle could not be turned on at all, so the magic finger, TAB and the floating
  button all stopped with it. Key filtering is the capability a keylogger would need, and a
  sideloaded app that asks for it is gated behind "Allow restricted settings" — which that phone
  did not offer in the App info menu, leaving no way through.

  The cost of asking is therefore not a scarier warning, as build 124 assumed. It is the entire
  service, including the parts that had nothing to do with keys.

  The description is shown verbatim in the system Accessibility settings as the user-facing disclosure.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeViewFocused|typeViewClicked|typeViewTextSelectionChanged|typeWindowStateChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows|flagReportViewIds|flagInputMethodEditor"
    android:canRetrieveWindowContent="true"
    android:canPerformGestures="true"
    android:notificationTimeout="0"
    android:description="@string/dictate__accessibility_service_description"
    android:summary="@string/dictate__accessibility_service_summary" />
